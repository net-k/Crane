using System;
using UnityEngine;
using UnityEngine.UI;

namespace SushiGame.Help
{
    public class HelpDialogView : MonoBehaviour
    {
        [SerializeField]
        Button _closeButton;

        public Button CloseButton => _closeButton;

        private void Awake()
        {
            
        }

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
